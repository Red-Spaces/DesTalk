import React from "react";

const NewChat = () => (
  <div className="relative w-full flex flex-col h-full">
    <div className="flex items-center justify-center h-screen">
      Add Features and landing details here
    </div>
  </div>
);

export default NewChat;
